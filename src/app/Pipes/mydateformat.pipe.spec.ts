import { MydateformatPipe } from './mydateformat.pipe';

describe('MydateformatPipe', () => {
  it('create an instance', () => {
    const pipe = new MydateformatPipe();
    expect(pipe).toBeTruthy();
  });
});
