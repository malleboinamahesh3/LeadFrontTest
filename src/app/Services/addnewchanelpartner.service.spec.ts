import { TestBed } from '@angular/core/testing';

import { AddnewchanelpartnerService } from './addnewchanelpartner.service';

describe('AddnewchanelpartnerService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: AddnewchanelpartnerService = TestBed.get(AddnewchanelpartnerService);
    expect(service).toBeTruthy();
  });
});
