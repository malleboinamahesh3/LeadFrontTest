import { MycurrencyFormatterDirective } from './mycurrency-formatter.directive';

describe('MycurrencyFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new MycurrencyFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
